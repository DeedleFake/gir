{{if .Element.IsMethod}}
	func (s *{{.Type.GetName}}) {{toCamelCase .Element.GetName}}({{.Arguments}}) {
		{{- .ConvertArguments}}
		{{.Call "s" -}}
	}
{{else}}
	func {{.Type.GetName}}{{toCamelCase .Element.GetName}}({{.Arguments}}) {
		panic("Not implemented.")
	}
{{end}}
