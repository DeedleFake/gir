{{if not .Type.IsForeign}}
	type {{.Type.GetName}} struct {
		_ structs.HostLayout
		_ [{{.Type.GetSize}}]byte
		{{range .Type.GetFields}}
			// {{.GetName}}
		{{end}}
	}

	func (s *{{.Type.GetName}}) c() *C.{{.CName}} {
		return (*C.{{.CName}})(unsafe.Pointer(s))
	}

	{{range .Type.GetMethods}}
		{{$.Generate "func" $.Type .}}
	{{end}}
{{end}}
